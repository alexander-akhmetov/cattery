{
  "name": "cattery",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Toolchain for extensions/cattery.ts, the pi extension, and opencode/cattery.ts, the opencode plugin. The kitty files, the state writer and the picker need none of this.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/cattery_extension_test.ts tests/cattery_opencode_test.ts"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.84.1",
    "@opencode-ai/plugin": "^1.18.22",
    "@types/node": "^26.2.0",
    "typescript": "^5.7.0"
  }
}
